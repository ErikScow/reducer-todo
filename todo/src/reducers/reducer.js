const reducer = (state, action) => {
    switch (action.type){

        default:
            return state
    }
}

//initial state:
const todo = [
    {
        item: 'Learn about reducers',
        completed: false,
        id: 3892987589
    }
]

export {reducer, todo}